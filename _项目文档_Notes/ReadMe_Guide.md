# 📱 无损剪辑 - 小白使用与部署说明书

## 🚀 快速部署（GitHub 云打包）

### 第一步：上传代码到 GitHub
1. 在 GitHub 新建一个仓库（仓库名**必须全小写**，如 `video-lossless-cutter`）
2. 将本项目所有文件 Push 到仓库

### 第二步：运行打包
1. 在仓库页面点击 **Actions** 标签
2. 选择 **Flutter Build Release** 工作流
3. 点击 **Run workflow** 按钮
4. 等待构建完成（约 5-10 分钟）
5. 下载 **Artifacts** 中的 APK 安装包

---

## 📂 项目文件结构

```text
├── pubspec.yaml                # 依赖配置（最重要！）
├── lib/
│   ├── main.dart               # APP 入口
│   ├── screens/
│   │   ├── home_page.dart      # 首页
│   │   ├── video_cut_page.dart # 剪切页
│   │   └── video_merge_page.dart # 合并页
│   └── utils/
│       └── ffmpeg_service.dart # FFmpeg 核心服务
├── assets/images/              # 图标资源（放 icon.png）
└── .github/workflows/build.yml # 云打包脚本
```

---

## ⚙️ 可修改参数说明

### 1. APP 名称修改

**修改位置 1**：`lib/main.dart` 顶部
```dart
const String appName = '无损剪辑'; // 👈 改这里
```

**修改位置 2**：`.github/workflows/build.yml` 顶部
```yaml
env:
  APP_LABEL: "无损剪辑"    # 👈 安装后显示的名称
  FILE_NAME: "video_lossless_cutter"  # 👈 下载文件名（英文）
```

### 2. 主题色修改

**位置**：`lib/main.dart`
```dart
const Color primaryColor = Color(0xFF6750A4); // 👈 修改颜色代码
```

### 3. 自定义图标

将 **1024x1024 像素**的正方形 PNG 图片放到：
```
assets/images/icon.png
```
云打包时会自动生成各尺寸图标。

---

## 📦 本地运行（可选）

如果你有 Flutter 本地环境：

```bash
# 1. 安装依赖
flutter pub get

# 2. 运行到手机
flutter run

# 3. 打包 APK
flutter build apk --release
```

---

## ❓ 常见问题

**Q: 打包失败报 Kotlin 版本错误？**
A: 脚本已自动修复，正常不会出现。如果仍报错，检查 `build.yml` 中的 `Fix Android Environment` 步骤是否完整。

**Q: APK 安装后图标是默认的？**
A: 确保 `assets/images/icon.png` 存在且符合规格。

**Q: 剪切出来的视频开头多几秒？**
A: 这是无损剪切的正常现象（只能在关键帧切割），非 BUG。
