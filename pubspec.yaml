name: lofter_fixer
description: LOFTER Watermark Remover
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  image_picker: ^1.0.7
  file_picker: ^6.1.1
  permission_handler: ^11.3.0
  fluttertoast: ^8.2.4
  #以此支持部分相册查看功能
  path_provider: ^2.1.2 

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0
  # 👇 图标生成工具
  flutter_launcher_icons: ^0.13.1

flutter:
  uses-material-design: true
  assets:
    - assets/images/

# 👇 图标配置
flutter_launcher_icons:
  android: "launcher_icon"
  ios: false
  image_path: "assets/images/icon.png" # 确保你上传了这张图
  min_sdk_android: 24