name: my_diary_app
description: A timeline diary app with features.
publish_to: 'none'
version: 1.5.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  # 👇 核心依赖库
  flutter_localizations: # 修复日期报错必须
    sdk: flutter
  intl: ^0.18.0          # 时间格式化
  shared_preferences: ^2.2.0 # 本地存储
  share_plus: ^7.2.0     # 导出/分享文件
  file_picker: ^6.1.1    # 导入/选择文件
  cupertino_icons: ^1.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_launcher_icons: ^0.13.1 # 自动化图标工具

# 👇 图标配置 (运行 dart run flutter_launcher_icons 生成)
flutter_launcher_icons:
  android: "launcher_icon"
  ios: false
  image_path: "assets/images/icon.png"
  min_sdk_android: 21

flutter:
  uses-material-design: true
  
  # 👇 资源注册
  assets:
    - assets/images/
  
  # 👇 字体注册 (确保 assets/fonts/myfont.ttf 存在)
  fonts:
    - family: MyFont
      fonts:
        - asset: assets/fonts/myfont.ttf