name: xhs_downloader_app
description: A XHS Downloader capable of extracting high-quality media via WebView.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.1.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  
  # UI Icons
  cupertino_icons: ^1.0.6
  
  # Browser Core (Crawling)
  webview_flutter: ^4.7.0
  
  # Network (Downloading)
  dio: ^5.4.1
  
  # System Capability
  permission_handler: ^11.3.0
  path_provider: ^2.1.2
  image_gallery_saver: ^2.0.3 # Replaced gal due to build error
  
  # Utils
  path: ^1.9.0
  shared_preferences: ^2.2.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0
  
  # Assets Generator (Disabled until assets are uploaded)
  # flutter_launcher_icons: ^0.13.1
  # flutter_native_splash: ^2.3.1

flutter:
  uses-material-design: true
  
  # Assets Registration
  # assets:
  #   - assets/images/

# Icon Configuration (Commented out)
# flutter_launcher_icons:
#   android: "launcher_icon"
#   ios: false
#   image_path: "assets/images/icon.png"
#   min_sdk_android: 21

# Splash Configuration (Commented out)
# flutter_native_splash:
#   color: "#FFFFFF"
#   image: "assets/images/splash.png"
#   android_12:
#     color: "#FFFFFF"
#     image: "assets/images/splash.png"
