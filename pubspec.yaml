name: video_cutter
description: "无损视频剪切与智能拼接工具"
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

# ===============================
# 依赖配置 (不使用 ffmpeg_kit，改用原生调用)
# ===============================
dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8
  
  # 文件选择器
  file_picker: ^6.1.1
  
  # 权限管理
  permission_handler: ^11.1.0
  
  # 路径获取
  path_provider: ^2.1.1
  
  # 本地存储 (设置保存)
  shared_preferences: ^2.2.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

# ===============================
# Flutter 资源配置
# ===============================
flutter:
  uses-material-design: true
  
  assets:
    - assets/bin/
