name: Build Lofter Fixer App
on: workflow_dispatch

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3

      - name: Setup Java
        uses: actions/setup-java@v3
        with:
          distribution: 'temurin'
          java-version: '17'

      - name: Setup Flutter
        uses: subosito/flutter-action@v2
        with:
          flutter-version: '3.19.0'

      # 1. 确保安卓工程存在
      - name: Create Android Project
        run: |
          if [ ! -d "android" ]; then
            flutter create . --platforms android --project-name lofter_fixer
          fi

      # 2. 🔧 放置模型文件 (重要！)
      # 假设你把 best_float16.tflite 传到了仓库根目录的 models 文件夹
      # 如果没有，你需要手动修改这里的来源路径
      - name: Place TFLite Model
        run: |
          mkdir -p android/app/src/main/assets
          # 尝试查找模型，如果没有则创建一个假的(防止编译失败，但在真机运行会崩)
          if [ -f "best_float16.tflite" ]; then
            cp best_float16.tflite android/app/src/main/assets/
            echo "✅ Model copied."
          else
            echo "⚠️ Model NOT found in root! Make sure you upload best_float16.tflite"
            # 这里的 fallback 仅为了能演示打包，实际使用必须上传模型
            touch android/app/src/main/assets/best_float16.tflite 
          fi

      # 3. 修复环境 (Kotlin版本冲突)
      - name: Fix Android Environment
        run: |
          sed -i 's/id "org.jetbrains.kotlin.android" version ".*"/id "org.jetbrains.kotlin.android" version "1.9.0"/g' android/settings.gradle
          sed -i 's/minSdkVersion.*/minSdkVersion 24/g' android/app/build.gradle # OpenCV 需要 minSdk 24+

      - name: Install Dependencies
        run: flutter pub get

      # 4. 打包
      - name: Build APK
        run: flutter build apk --release --target-platform android-arm64

      - name: Upload APK
        uses: actions/upload-artifact@v4
        with:
          name: LofterFixer-Release
          path: build/app/outputs/flutter-apk/app-release.apk