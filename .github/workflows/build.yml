name: Build Lofter Fixer (Respect User Config)
on: workflow_dispatch

env:
  APP_NAME: "LOFTER去水印"

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3

      - name: Setup Java
        uses: actions/setup-java@v3
        with:
          distribution: 'temurin'
          java-version: '17'

      - name: Setup Flutter
        uses: subosito/flutter-action@v2
        with:
          flutter-version: '3.19.0'

      # 👇👇👇 关键变化：不再 rm -rf android 👇👇👇
      # 我们假设你已经上传了修复好的 AndroidManifest.xml 和 MainActivity.kt
      
      # 1. 放置模型 (防止你忘了传)
      - name: Place TFLite Model
        run: |
          mkdir -p android/app/src/main/assets
          if [ -f "best_float16.tflite" ]; then
            cp best_float16.tflite android/app/src/main/assets/
            echo "✅ Model copied."
          else
            echo "❌ Error: best_float16.tflite not found!"
            touch android/app/src/main/assets/best_float16.tflite
          fi

      # 2. 注入核心依赖 (防止 build.gradle 没配置好)
      # 如果你的 build.gradle 已经是好的，这一步也不会破坏它
      - name: Ensure Dependencies
        run: |
          # 检查是否包含 OpenCV，如果没有，则注入到 build.gradle
          if ! grep -q "com.quickbirdstudios:opencv" android/app/build.gradle; then
             echo "🔧 Injecting Native Dependencies..."
             sed -i '/dependencies {/a \    implementation "org.jetbrains.kotlin:kotlin-stdlib:1.9.0"\n    implementation "org.tensorflow:tensorflow-lite:2.14.0"\n    implementation "org.tensorflow:tensorflow-lite-support:0.4.4"\n    implementation "com.quickbirdstudios:opencv:4.5.3.0"\n    implementation "org.jetbrains.kotlinx:kotlinx-coroutines-android:1.7.1"' android/app/build.gradle
          else
             echo "✅ Dependencies already present."
          fi

          # 确保 Kotlin 版本正确
          sed -i 's/id "org.jetbrains.kotlin.android" version ".*"/id "org.jetbrains.kotlin.android" version "1.9.0"/g' android/settings.gradle

      # 3. 改名与图标
      - name: Setup App Metadata
        run: |
          # 仅当你的 Manifest 里没有写死 label 时，脚本才会尝试改名
          # 但因为你本地 Manifest 已经写了 label，这步其实是保险
          dart run flutter_launcher_icons

      - name: Install Dependencies
        run: flutter pub get

      - name: Build APK
        run: flutter build apk --release --target-platform android-arm64

      - name: Upload APK
        uses: actions/upload-artifact@v4
        with:
          name: LofterFixer-Stable
          path: build/app/outputs/flutter-apk/app-release.apk